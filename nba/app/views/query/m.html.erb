<div class=row-fluid>
  <div class=span6>
    <h2>Result of query <%= params[:action].upcase %></h2>
    <p>
      For coaches who coached at most 7 seasons but more than 1 season, who are the three more successful? (Success rate is season win percentage: <code>season_win / (season_win + season_loss)</code>). Be sure to count all seasons when computing the percentage.
    </p>
    <table class="table table-striped">
      <thead>
        <tr>
          <th scope=col>#</th>
          <th scope=col>First Name</th>
          <th scope=col>Last Name</th>
          <th scope=col>Success rate</th>
        </tr>
      </thead>
      <tbody>
        <%- while line = @results.fetch() -%>
          <tr>
            <th scope=row><%= link_to(
              line[0],
              :controller => "person",
              :action => "coach",
              :id => line[0]
            ) %></th>
            <td><%= line[1] %></td>
            <td><%= line[2] %></td>
            <td><%= line[3].round(3) * 100 %> %</td>
          </tr>
        <%- end -%>
      </tbody>
    </table>
  </div>
  <%= render :partial => "sql" %>
</div>
